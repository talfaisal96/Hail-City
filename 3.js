function Hail() {
  alert("أهلا و سهلاً بكم")}

  function getConfirmation() {
  var retVal = confirm("هل اعجبتك الجولة ?");
  if( retVal == true ) {
  document.write ("شكراً جزيلاً");
  return true; }
  else { document.write ("شكراً جزيلاً");
  return false; } }
